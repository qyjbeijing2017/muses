Shader "Test" {
    Properties {
        
    }
    
    SubShader {
        Pass {
            // renderstate

            // glsl
            GLSLPROGRAM

            attribute lowp float position=2, rotation;

            void vert() {
                gl_Position = vec4(0.0,0.0,0.0,1).xyz
                // gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
            }

            // void frag() {
            //     gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
            // }

            ENDGLSL
        }
    }

    FallBack "Diffuse"
}