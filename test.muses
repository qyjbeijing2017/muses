Shader "Test" {
    Properties {
        
    }
    
    SubShader {
        Pass {
            // renderstate

            // glsl
            GLSLPROGRAM

            attribute lowp float position;

            void vert() {
                // gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
            }

            // void frag() {
            //     gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
            // }

            ENDGLSL
        }
    }

    FallBack "Diffuse"
}