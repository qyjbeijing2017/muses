Shader "Test" {
    Properties {
        _Int("Int", Int) = 2
        _Float("Float", Float) = 1.5
        _Range("Range", Range(2.0,3.0)) = 3.0

        _Color("Color", Color) = (1,1,1,1)
        _Vector("Vector", Vector) = (2,3,4,5)
        _2D("2D", 2D) = "white" {}
        _3D("3D", 3D) = "black" {}
    }
    
    SubShader {
        Pass {
            // renderstate

            // glsl
            GLSLPROGRAM

            void vert(in lowp vec4 vertex) {
                gl_Position = vec4(0.0,0.0,0.0,1).xyz;
                if(a){

                } else {
                    
                }

                // gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
            }

            // void frag() {
            //     gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
            // }

            ENDGLSL
        }
    }

    FallBack "Diffuse"
}